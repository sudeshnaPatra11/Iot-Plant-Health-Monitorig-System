# Machine Learning

The project report states that the ML module predicted moisture-stress events with 91% accuracy up to four hours in advance.

The original model-training code is currently unavailable. This folder intentionally contains no fabricated implementation.

When the original code is recovered, add the training code, dataset information, preprocessing, features and evaluation details here.
